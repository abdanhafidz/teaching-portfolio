#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int> v;
    v.push_back(5);
    v.insert(v.begin(),3);
    
    for(auto isi: v){
        cout<<isi<<endl;
    }
    cout<<v[0];
    // Lower Bound, Upper Bound

}